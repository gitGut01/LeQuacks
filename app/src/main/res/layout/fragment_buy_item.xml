<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#8BC34A"
    android:gravity="center_horizontal">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAnimals"
        android:layout_width="match_parent"
        android:layout_height="580dp"
        android:layout_marginBottom="8dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:layout_editor_absoluteX="0dp">

    </androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:id="@+id/tv_credits"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:layout_marginBottom="4dp"
        android:text="Credits: 0"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/rvAnimals"
        app:layout_constraintStart_toStartOf="@+id/rvAnimals" />

    <Button
        android:id="@+id/btn_ruby_store"
        android:layout_width="133dp"
        android:layout_height="33dp"
        android:layout_marginStart="160dp"
        android:layout_marginLeft="160dp"
        android:text="The ruby store"
        android:textSize="12sp"
        app:layout_constraintBottom_toTopOf="@+id/rvAnimals"
        app:layout_constraintStart_toEndOf="@+id/tv_credits" />

    <TextView
        android:id="@+id/tv_buy_limit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="72dp"
        android:layout_marginLeft="72dp"
        android:text="0/2"
        app:layout_constraintStart_toEndOf="@+id/tv_credits"
        app:layout_constraintTop_toTopOf="@+id/tv_credits" />

</androidx.constraintlayout.widget.ConstraintLayout>